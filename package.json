{
  "name": "local-port-manager",
  "version": "0.1.0",
  "description": "Web UI to see and manage locally listening TCP ports (active / pending / stale) and kill their processes.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
